#include<stdio.h>
int main()
{
  int n,last,sum=0;
  scanf("%d",&n);
  while(n!=0)
  {
  last=n%10;
  sum=sum+(last*last);
  n=n/10;
  }
  if(sum==1)
  printf("true");
  else
  printf("false");
    
}
